import { getFutureToDos } from './lab-four';

describe('getFutureToDos', () => {
  // utility function to help create a delay
  const wait = ms => new Promise(resolve => setTimeout(resolve, ms));
  
  // your code here.
});
